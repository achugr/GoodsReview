<?xml version="1.0" encoding="UTF-8"?>
 
<config charset="ISO-8859-1">
    
    <include path="functionsCitilink.xml"/>
                
    <!-- collects all tables for individual products -->
    <var-def name="products">    
        <call name="download-multipage-list">
            <call-param name="pageUrl">http://www.citilink.ru/catalog/computers_and_notebooks/notebooks/</call-param>
            <call-param name="nextXPath">//div[@class="cat_nav top"]/*/a[text() = ((..//span/text())+1)]/@href</call-param>
            <call-param name="itemXPath">//table[@class = "item"]</call-param>
            <call-param name="maxloops">38</call-param>
        </call>
    </var-def>
    
    <!-- iterates over all collected products and extract desired data -->
    <file action="write" path="citilink/catalog.xml" charset="UTF-8">
        <![CDATA[ <catalog> ]]>
        <loop item="item" index="i">
            <list><var name="products"/></list>
            <body>
                <xquery>
                    <xq-param name="item" type="node()"><var name="item"/></xq-param>
                    <xq-expression><![CDATA[
                            declare variable $item as node() external;
 
                            let $name := data($item//*[@class='l'])
                            let $desc := data($item//*[@class='descr'])
                            let $price := data($item//*[@class='price'])
                                return
                                    <product>
                                        <name>{normalize-space($name)}</name>
                                        <desc>{normalize-space($desc)}</desc>
                                        <price>{normalize-space($price)}</price>
                                    </product>
                    ]]></xq-expression>
                </xquery>
            </body>
        </loop>
        <![CDATA[ </catalog> ]]>
    </file>
 
</config>
